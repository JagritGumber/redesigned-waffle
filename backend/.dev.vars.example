# The Cloudflare Worker backend is experimental. The stable self-host backend for Solid is ../manager.
# Copy this file to backend/.dev.vars and fill regenerated local values there.
# Do not commit backend/.dev.vars.

# Secrets: regenerate these and keep them only in .dev.vars locally or Wrangler secrets in production.
AUTH_SECRET=
RUNPOD_API_KEY=
MODEL_IMAGE_REBUILD_GITHUB_TOKEN=
MODEL_IMAGE_WEBHOOK_TOKEN=

# Non-secret Worker vars and binding-related names. These may also live in wrangler.jsonc.
RUNPOD_GENERATOR_ID=
RUNPOD_DOWNLOADER_ID=
RUNPOD_WEBHOOK_URL=
MODEL_IMAGE_RUNPOD_BUILD_POLLING=true
R2_PUBLIC_BUCKET_URL=
R2_BUCKET_NAME=

# Cloudflare Workers cannot commit model migrations to a private mirror clone.
# Use the manager backend for private mirror model installs, or explicitly opt into GitHub below.
MODEL_IMAGE_REBUILD_PROVIDER=

# Optional GitHub provider for private repos or non-sensitive installs only.
# It commits model migration metadata and creates model release tags in GitHub.
MODEL_IMAGE_REBUILD_ALLOW_GITHUB_METADATA=false
MODEL_IMAGE_REBUILD_GITHUB_REPOSITORY=
