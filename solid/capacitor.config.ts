import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.jagritgumber.selfhoststudio",
  appName: "Selfhost Studio",
  webDir: "dist",
};

export default config;
